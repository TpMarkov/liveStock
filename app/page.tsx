import { Button } from "@/components/ui/button";
import React from "react";

const Home = () => {
  return (
    <div className="flex items-center justify-center mt-auto h-[100vh]">
      <Button>Click me</Button>
    </div>
  );
};

export default Home;
